// Real-time data fetching - No hardcoded data
// All station and train data is fetched from the database via API

module.exports = {
  // Data is now fetched dynamically from the database
  // Use API endpoints:
  // GET /api/stations - Fetch all stations
  // GET /api/trains - Fetch all trains
  // GET /api/trains/search - Search trains by route
};
